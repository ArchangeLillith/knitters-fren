import React from "react";

interface UpdatePatternProps {}

const UpdatePattern = (props: UpdatePatternProps) => {
	return (
		<div>
			<h1>UpdatePattern component rendered</h1>
		</div>
	);
};

export default UpdatePattern;
