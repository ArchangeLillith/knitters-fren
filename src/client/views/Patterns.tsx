import React from "react";

interface PatternsProps {}

const Patterns = (props: PatternsProps) => {
	return (
		<div>
			<h1>Patterns component rendered</h1>
		</div>
	);
};

export default Patterns;
